#include "catch.hpp"
#include "Equipe.hpp"

TEST_CASE( "test Equipe", "[Equipe]" ) {
	//Equipe(int eq, int bat_id): numero(eq), bourse(30), hab(eq, bat_id), tour(eq,bat_id+1){}
	//Equipe E =  Equipe(1,2);
	//REQUIRE( E.getBourse() == 30);
	//E.ajoutMonaie(50);
	//REQUIRE( E.getBourse() == 80 );
	
	
}
