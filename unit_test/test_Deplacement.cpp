#include "catch.hpp"
#include "Deplacement.hpp"


TEST_CASE("Creation Deplacement", "[Deplacement]"){
	/*Unite U = Unite();
	sf::Vector2f bouge = sf::Vector2f (10,10);
	Deplacement D = Deplacement(&U, bouge);
	REQUIRE( U.getPos() == sf::Vector2f (9,9) );*/
}
